---
filebeat.modules:
  - module: system
    syslog:
      enabled: true
filebeat.prospectors:
  - input_type: log
    paths:
      - /var/log/*.log
output.logstash:
  hosts:
    - {{ elasticsearch_host }}:5044
logging.level: error
